ServerAliveInterval 300
ServerAliveCountMax 3

host github.com
  IdentityFile ~/.ssh/github.com.pub
  IdentitiesOnly yes
Host git.jpatrick.io
  Port 222
  IdentityFile ~/.ssh/git.jpatrick.io
Host *
  UseRoaming no

# Manually import edit the local file to import content.
# git-crypt can cause issues where it will attempt to import encrypted binary as plain-text
Include config.d/local
